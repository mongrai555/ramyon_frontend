# Copy to .env.local (development) or .env.production (deploy).
# NEXT_PUBLIC_* values are baked into the bundle at BUILD time, so the host
# must have this set before `next build` runs — setting it at runtime is too late.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3000/api
